# Runner 配置示例。复制为 config.yaml 后修改。
# 安全注意:allowed_roots / blocked_paths 只在本地生效,服务器无法远程修改(docs/security.md §2)

server_url: http://127.0.0.1:8700
machine_name: my-machine
enrollment_token: dev-enroll-token   # 仅首次注册用,注册后可删除

allowed_roots:
  - ~/projects

blocked_paths: []

# 启用的插件(只来自本地配置,服务器不能远程改)。
# 内置:exec(remote_exec)、file(读写/patch/列文件)。可选:sysinfo(remote_sysinfo)等。
# 不填=默认 [exec, file]。每个插件声明的工具会自动上报给服务器并暴露给模型。
plugins: [exec, file]

heartbeat_interval_seconds: 10
